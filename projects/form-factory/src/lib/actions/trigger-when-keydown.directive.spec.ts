import { TriggerWhenKeyIsDownDirective } from './trigger-when-key-is-down.directive';

describe('TriggerWhenKeyIsDownDirective', () => {
  it('should create an instance', () => {
    const directive = new TriggerWhenKeyIsDownDirective();
    expect(directive).toBeTruthy();
  });
});
